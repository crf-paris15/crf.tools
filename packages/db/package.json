{
  "name": "@repo/db",
  "scripts": {
    "db:generate": "prisma generate --no-hints",
    "db:migrate": "prisma migrate dev -n migration",
    "db:deploy": "prisma migrate deploy",
    "db:format": "prisma format",
    "db:validate": "prisma validate"
  },
  "dependencies": {
    "@prisma/adapter-pg": "~7.0.1",
    "@prisma/client": "~7.0.0"
  },
  "devDependencies": {
    "prisma": "^7.0.0"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "type": "module"
}
